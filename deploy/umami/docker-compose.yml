# Umami analytics. Runs on the PUBLIC VPS, not the app server -- that keeps
# tracker traffic off the WireGuard tunnel entirely.
#
# The VPS does NOT need a checkout of this repo. Put this one file in a
# directory there (scp it, or paste it), create a .env beside it holding
# POSTGRES_PASSWORD and APP_SECRET, then:
#
#   docker compose up -d
#
# It binds 127.0.0.1 only. nginx proxies the two public paths it needs
# (see docs/ANALYTICS.md); the dashboard stays unreachable from the internet.

services:
  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    restart: unless-stopped
    # Loopback only. Nothing here should be directly reachable.
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      DATABASE_URL: postgresql://umami:${POSTGRES_PASSWORD}@db:5432/umami
      DATABASE_TYPE: postgresql
      APP_SECRET: ${APP_SECRET}
      # Serves the tracker at /script.js, which nginx re-exposes on your
      # own domain as /stats.js so ad blockers do not strip it.
      TRACKER_SCRIPT_NAME: script
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - umami-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U umami"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  umami-db:
